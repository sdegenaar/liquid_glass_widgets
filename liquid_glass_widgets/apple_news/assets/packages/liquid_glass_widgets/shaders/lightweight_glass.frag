{
  "format_version": 1,
  "sksl": {
    "entrypoint": "lightweight_glass_fragment_main",
    "shader": "// This SkSL shader is autogenerated by spirv-cross.\n\nfloat4 flutter_FragCoord;\n\nuniform vec4 uData0;\nuniform vec4 uData1;\nuniform vec4 uData2;\nuniform vec4 uData3;\nuniform vec4 uData4;\nuniform vec4 uData5;\nuniform vec4 uData6;\n\nvec4 fragColor;\n\nvec2 FLT_flutter_local_FlutterFragCoord()\n{\n    return flutter_FragCoord.xy;\n}\n\nvec3 FLT_flutter_local_applyGlassColorLW(vec3 liquidColor, vec4 glassColor)\n{\n    float backdropLuminance = dot(liquidColor, vec3(0.2989999949932098388671875, 0.58700001239776611328125, 0.114000000059604644775390625));\n    float glassLuminance = dot(glassColor.xyz, vec3(0.2989999949932098388671875, 0.58700001239776611328125, 0.114000000059604644775390625));\n    vec3 tinted = clamp(glassColor.xyz + vec3(backdropLuminance - glassLuminance), vec3(0.0), vec3(1.0));\n    float chroma = max(max(glassColor.x, glassColor.y), glassColor.z) - min(min(glassColor.x, glassColor.y), glassColor.z);\n    float chromaWeight = clamp(chroma * 8.0, 0.0, 1.0);\n    vec3 directMix = mix(liquidColor, glassColor.xyz, vec3(glassColor.w));\n    vec3 luminosityMix = mix(liquidColor, tinted, vec3(glassColor.w));\n    return mix(directMix, luminosityMix, vec3(chromaWeight));\n}\n\nvoid FLT_main()\n{\n    vec2 uSize = uData0.xy;\n    vec2 uOrigin = uData0.zw;\n    vec4 uGlassColor = uData1;\n    float uThickness = uData2.x;\n    vec2 uLightDirection = uData2.yz;\n    float uLightIntensity = uData2.w;\n    float uAmbientStrength = uData3.x;\n    float uSaturation = uData3.y;\n    float uRefractiveIndex = uData3.z;\n    float uChromaticAberration = uData3.w;\n    float uCornerRadius = uData4.x;\n    vec2 uScale = uData4.yz;\n    float uGlowIntensity = uData4.w;\n    float uDensityFactor = uData5.x;\n    float uIndicatorWeight = uData5.y;\n    float uSpecularSharpnessF = uData5.z;\n    float uBackdropLuma = uData5.w;\n    vec2 pixelCoord = FLT_flutter_local_FlutterFragCoord();\n    vec2 localLogical = (pixelCoord - uOrigin) / uScale;\n    vec2 halfSize = uSize * 0.5;\n    vec2 p = localLogical - halfSize;\n    float r;\n    if (uCornerRadius < 0.0)\n    {\n        bool _185 = p.x < 0.0;\n        bool _191;\n        if (_185)\n        {\n            _191 = p.y < 0.0;\n        }\n        else\n        {\n            _191 = _185;\n        }\n        if (_191)\n        {\n            r = uData6.x;\n        }\n        else\n        {\n            bool _201 = p.x >= 0.0;\n            bool _207;\n            if (_201)\n            {\n                _207 = p.y < 0.0;\n            }\n            else\n            {\n                _207 = _201;\n            }\n            if (_207)\n            {\n                r = uData6.y;\n            }\n            else\n            {\n                bool _215 = p.x >= 0.0;\n                bool _221;\n                if (_215)\n                {\n                    _221 = p.y >= 0.0;\n                }\n                else\n                {\n                    _221 = _215;\n                }\n                if (_221)\n                {\n                    r = uData6.z;\n                }\n                else\n                {\n                    r = uData6.w;\n                }\n            }\n        }\n    }\n    else\n    {\n        r = uCornerRadius;\n    }\n    vec2 q = (abs(p) - halfSize) + vec2(r);\n    vec2 maxQ = max(q, vec2(0.0));\n    float maxQLen = length(maxQ);\n    float dist = (maxQLen + min(max(q.x, q.y), 0.0)) - r;\n    float smoothing = 1.0 / uScale.x;\n    float mask = 1.0 - smoothstep(-smoothing, smoothing, dist);\n    if (mask <= 0.0)\n    {\n        fragColor = vec4(0.0);\n        return;\n    }\n    bool isEdge = maxQLen > 0.00999999977648258209228515625;\n    vec2 _283;\n    if (isEdge)\n    {\n        _283 = (sign(p) * maxQ) / vec2(maxQLen);\n    }\n    else\n    {\n        _283 = vec2(0.0);\n    }\n    vec2 surfaceNormal = _283;\n    float normalZ = sqrt(max(0.0, 1.0 - dot(surfaceNormal, surfaceNormal)));\n    float effectiveBorder = 0.5 + (uIndicatorWeight * 0.5);\n    float effectiveSmoothing = smoothing * (1.0 + (uIndicatorWeight * 0.5));\n    float borderMask = 1.0 - smoothstep(0.0, effectiveSmoothing, abs(dist) - effectiveBorder);\n    float densityFactor = uDensityFactor;\n    float thicknessNorm = uThickness / 10.0;\n    float densitySpecularBoost = (1.0 + ((thicknessNorm - 1.0) * 0.1500000059604644775390625)) * (1.0 + (densityFactor * 0.20000000298023223876953125));\n    int specLevel = int(floor(uSpecularSharpnessF + float(0.5)));\n    vec2 _346;\n    if (isEdge)\n    {\n        _346 = (surfaceNormal + (vec2(-surfaceNormal.y, surfaceNormal.x) * 0.20000000298023223876953125)) * 0.980580627918243408203125;\n    }\n    else\n    {\n        _346 = vec2(0.0);\n    }\n    vec2 anisoN = _346;\n    float lightCatch = max(dot(anisoN, uLightDirection), 0.0);\n    float kickCatch = max(dot(anisoN, -uLightDirection), 0.0);\n    float keySpecular;\n    float kickSpecular;\n    if (specLevel == 0)\n    {\n        float lc2 = lightCatch * lightCatch;\n        float lc4 = lc2 * lc2;\n        keySpecular = lc4 * lc4;\n        float kc2 = kickCatch * kickCatch;\n        float kc4 = kc2 * kc2;\n        kickSpecular = kc4 * kc4;\n    }\n    else\n    {\n        if (specLevel == 1)\n        {\n            float lc2_1 = lightCatch * lightCatch;\n            float lc4_1 = lc2_1 * lc2_1;\n            float lc8 = lc4_1 * lc4_1;\n            keySpecular = lc8 * lc8;\n            float kc2_1 = kickCatch * kickCatch;\n            float kc4_1 = kc2_1 * kc2_1;\n            float kc8 = kc4_1 * kc4_1;\n            kickSpecular = kc8 * kc8;\n        }\n        else\n        {\n            float lc2_2 = lightCatch * lightCatch;\n            float lc4_2 = lc2_2 * lc2_2;\n            float lc8_1 = lc4_2 * lc4_2;\n            float lc16 = lc8_1 * lc8_1;\n            keySpecular = lc16 * lc16;\n            float kc2_2 = kickCatch * kickCatch;\n            float kc4_2 = kc2_2 * kc2_2;\n            float kc8_1 = kc4_2 * kc4_2;\n            float kc16 = kc8_1 * kc8_1;\n            kickSpecular = kc16 * kc16;\n        }\n    }\n    keySpecular *= (uLightIntensity * densitySpecularBoost);\n    kickSpecular *= ((uLightIntensity * 0.4000000059604644775390625) * densitySpecularBoost);\n    float bodyIntensityBoost = 0.100000001490116119384765625 * (1.0 + (uLightIntensity * 0.1500000059604644775390625));\n    float effectiveAmbient = uAmbientStrength * (1.0 - (densityFactor * 0.300000011920928955078125));\n    vec3 bodyColor = uGlassColor.xyz * (effectiveAmbient + bodyIntensityBoost);\n    float bodyAlpha = (uGlassColor.w + (densityFactor * 0.1500000059604644775390625)) * mask;\n    float thicknessOffset = (uThickness - 10.0) / 10.0;\n    float totalSpecular = keySpecular + kickSpecular;\n    float rimBaseWithIntensity = max(0.5, (0.4000000059604644775390625 * uLightIntensity) * 0.60000002384185791015625);\n    float rimBrightness = (rimBaseWithIntensity + (thicknessOffset * 0.100000001490116119384765625)) + (densityFactor * 0.0500000007450580596923828125);\n    vec3 rimColorBase = vec3(1.0) * (rimBrightness + (totalSpecular * 0.60000002384185791015625));\n    float rimAlphaBase = 0.800000011920928955078125 * uRefractiveIndex;\n    rimAlphaBase += (totalSpecular * 0.5);\n    rimAlphaBase *= ((1.0 + (thicknessOffset * 0.1500000059604644775390625)) * (1.0 + (densityFactor * 0.100000001490116119384765625)));\n    rimAlphaBase *= borderMask;\n    rimAlphaBase = clamp(rimAlphaBase, 0.0, 1.0);\n    vec3 finalColor = mix(bodyColor, rimColorBase, vec3(rimAlphaBase));\n    finalColor += (vec3(0.0500000007450580596923828125) * uIndicatorWeight);\n    float finalAlpha = max(bodyAlpha, rimAlphaBase * 0.800000011920928955078125);\n    float glowMask = step(0.00999999977648258209228515625, uGlowIntensity);\n    vec3 glowContribution = ((vec3(1.0) * uGlowIntensity) * 0.300000011920928955078125) * glowMask;\n    finalColor = clamp(finalColor + glowContribution, vec3(0.0), vec3(1.0));\n    finalAlpha = max(finalAlpha, (uGlowIntensity * 0.300000011920928955078125) * glowMask);\n    vec3 param = finalColor;\n    vec4 param_1 = uGlassColor;\n    finalColor = FLT_flutter_local_applyGlassColorLW(param, param_1);\n    float adaptiveStrength = mix(1.2000000476837158203125, 0.800000011920928955078125, uBackdropLuma);\n    float adaptiveSaturation = uSaturation * adaptiveStrength;\n    float luminance = dot(finalColor, vec3(0.2989999949932098388671875, 0.58700001239776611328125, 0.114000000059604644775390625));\n    finalColor = mix(vec3(luminance), finalColor, vec3(adaptiveSaturation));\n    finalColor = clamp(finalColor, vec3(0.0), vec3(1.0));\n    float fresnel = (((1.0 - normalZ) * borderMask) * 0.100000001490116119384765625) * adaptiveStrength;\n    finalColor = clamp(finalColor + vec3(fresnel), vec3(0.0), vec3(1.0));\n    fragColor = vec4(finalColor * finalAlpha, finalAlpha);\n}\n\nhalf4 main(float2 iFragCoord)\n{\n      flutter_FragCoord = float4(iFragCoord, 0, 0);\n      FLT_main();\n      return fragColor;\n}\n",
    "stage": 1,
    "uniforms": [
      {
        "array_elements": 0,
        "bit_width": 32,
        "columns": 1,
        "location": 0,
        "name": "uData0",
        "rows": 4,
        "type": 10
      },
      {
        "array_elements": 0,
        "bit_width": 32,
        "columns": 1,
        "location": 1,
        "name": "uData1",
        "rows": 4,
        "type": 10
      },
      {
        "array_elements": 0,
        "bit_width": 32,
        "columns": 1,
        "location": 2,
        "name": "uData2",
        "rows": 4,
        "type": 10
      },
      {
        "array_elements": 0,
        "bit_width": 32,
        "columns": 1,
        "location": 3,
        "name": "uData3",
        "rows": 4,
        "type": 10
      },
      {
        "array_elements": 0,
        "bit_width": 32,
        "columns": 1,
        "location": 4,
        "name": "uData4",
        "rows": 4,
        "type": 10
      },
      {
        "array_elements": 0,
        "bit_width": 32,
        "columns": 1,
        "location": 5,
        "name": "uData5",
        "rows": 4,
        "type": 10
      },
      {
        "array_elements": 0,
        "bit_width": 32,
        "columns": 1,
        "location": 6,
        "name": "uData6",
        "rows": 4,
        "type": 10
      }
    ]
  }
}